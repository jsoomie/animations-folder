export enum defaults {
  SCALE = 1,
  OPACITY_ZERO = 0,
  OPACITY_FULL = 1,
  DELAY = 1,
  DURATION = 1,
  SPRING = "spring",
  TWEEN = "tween",
}

export enum one {
  FULL = 1,
  THIRD = 0.75,
  HALF = 0.5,
  QUARTER = 0.25,
  ZERO = 0,
}
